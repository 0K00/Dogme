{
  "name": "dogme",
  "version": "0.1.0",
  "description": "Modern & simple CSS framework",
  "main": "/lib/main.min.css",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "sass src/scss/main.scss:lib/main.css && sass src/scss/config.scss:lib/config.css && sass src/scss/main.scss:lib/main.min.css --style compressed && sass src/scss/config.scss:lib/config.min.css --style compressed",
    "main": "sass --watch src/scss/main.scss:lib/main.css",
    "config": "sass --watch src/scss/config.scss:lib/config.css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.50.0"
  }
}
