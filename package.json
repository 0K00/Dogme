{
  "name": "dogme",
  "version": "0.1.0",
  "description": "Modern & simple CSS framework",
  "main": "/lib/main.min.css",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "sass --watch src/scss/main.scss:lib/main.css",
    "min": "sass --watch src/scss/main.scss:lib/main.min.css --style compressed"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.50.0"
  }
}
